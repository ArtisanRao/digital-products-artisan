# From: C:\Users\Dadaba Rao\Documents\digital-products-artisan\libcd ..   # go to repo root# Ensure the dynamic route folder exists (the square brackets are literal)$path = 'app\categories\[slug]\page.tsx'New-Item -ItemType Directory -Path (Split-Path $path) -Force | Out-Null# Paste your clipboard contents into the file (UTF-8, no extra newline)Set-Content -LiteralPath $path -Value (Get-Clipboard) -NoNewline